# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so
.env
# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a script, but you may want to ignore
#  files generated by PyInstaller if you package the Python program.
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
htmlcov/ # Coverage HTML report

# End-to-end test / browser-based test reports
geckodriver.log

# Pyflow
.pyflow/

# VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# PyCharm
.idea/
*.iml
# misc.xml should be shared, but other workspace files can be ignored
# .idea/workspace.xml # Often user-specific, but can be shared if team agrees
# .idea/shelf/ # User-specific changes

# Virtual Environments
venv/
.venv/
env/
.env/
ENV/
.ENV/
# If you have a .env file for secrets, ensure it's listed here IF you don't want to commit a template
.env
     

# Operating System Files
.DS_Store
Thumbs.db
Desktop.ini

# SQLite database file (if it's large or contains sensitive/dynamic data)
# If it's small sample data for tests and meant to be shared, you can remove this line.
# data/ecommerce_support.db

# FAISS index files (if large and can be rebuilt)
# data/doc_index/*.faiss

# Log files
*.log
logs/

# Specific to this project if needed
# data/doc_index/ # If you prefer to generate this and not commit it
# evaluation_report.md # If you generate this locally and don't want to commit every version